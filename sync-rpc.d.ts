// sync-rpc.d.ts
declare module 'sync-rpc' {
    // Declare the types for `createClient` as any
    // You can be more specific if needed later
    export default function createClient(...args: any[]): any;
}
